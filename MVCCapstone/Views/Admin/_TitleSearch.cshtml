@model MVCCapstone.Models.ManageBookSearchModel

@if (Model.bookResults.Count() == 0)
{
 <h3>There are no results that matches that title</h3>
}
else
{
    <table class="table table-striped table-bordered">
        <tr>
            <th class="col-sm-2"></th>
            <th class="col-sm-1">Id</th>
            <th>Title</th>
            <th>Author</th>
            <th>Publisher</th>
        </tr>

        @foreach (MVCCapstone.Models.Book book in Model.bookResults)
         {
            <tr id ="@book.BookId">
                <td class="text-center">
                     @Ajax.ActionLink("E", "editprompt", new { bookid = book.BookId }, new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "result" }, new { @class = "btn btn-primary", onclick = "scrollTo('capstone')" })
                     @Ajax.ActionLink("D", "deleteprompt", new { bookid = book.BookId }, new AjaxOptions { HttpMethod = "POST", UpdateTargetId = "result" }, new { @class = "btn btn-primary", onclick = "scrollTo('capstone')" })
                </td>
                <td>@book.BookId</td>
                <td>@book.Title</td>
                <td>@book.Author</td>
                <td>@book.Publisher</td>
            </tr>
         }

    </table>
}